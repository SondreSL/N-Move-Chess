{
  "scripts": {
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.elm": [
      "elm-format --yes",
      "git add"
    ],
    "*.js": [
      "prettier --write",
      "git add"
    ]
  },
  "devDependencies": {
    "elm-format": "^0.8.2",
    "husky": "^3.0.9",
    "lint-staged": "^9.4.2"
  }
}
